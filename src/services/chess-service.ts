import { AlgebraicPosition } from 'src/model/algebraic-position';

export class ChessService {

    public async getPossibleMoves(startingPosition: AlgebraicPosition): Promise<AlgebraicPosition[]> {
        throw "Not implemented";
    }
    
}